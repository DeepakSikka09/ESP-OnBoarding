/*
package com.ecomexpress.oneBoarding.data.datasource.local

import javax.inject.Inject


class LocalDataSource @Inject constructor(private val espDatabase: ESPDatabase) {

    suspend fun insertUserInfo(profile: ESPProfile) =
        espDatabase.profileDao().insertProfile(profile)
}
*/
